<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>For My Love ❤️</title>

<style>
body{
    margin:0;
    font-family:'Segoe UI',sans-serif;
    overflow:hidden;
    background:linear-gradient(135deg,#ff9a9e,#fad0c4,#ffd1ff);
    display:flex;
    justify-content:center;
    align-items:center;
    height:100vh;
    text-align:center;
}

/* password screen */
#lockScreen{
    position:fixed;
    width:100%;
    height:100%;
    background:#fff0f5;
    display:flex;
    flex-direction:column;
    justify-content:center;
    align-items:center;
    z-index:100;
}

input{
    padding:10px;
    border-radius:8px;
    border:1px solid #aaa;
}

/* main card */
.container{
    display:none;
    background:white;
    padding:30px;
    border-radius:25px;
    width:90%;
    max-width:430px;
    box-shadow:0 15px 45px rgba(0,0,0,0.25);
    position:relative;
    z-index:10;
}

img{
    width:130px;
    height:130px;
    border-radius:50%;
    object-fit:cover;
    border:4px solid pink;
}

h2{
    color:#ff4081;
}

/* buttons */
button{
    padding:10px 22px;
    border:none;
    border-radius:10px;
    margin:8px;
    font-size:16px;
    cursor:pointer;
}

#yes{background:#4caf50;color:white;}
#no{background:#f44336;color:white;position:absolute;}

/* popup */
.popup{
    display:none;
    position:fixed;
    top:50%;
    left:50%;
    transform:translate(-50%,-50%);
    background:white;
    padding:30px;
    border-radius:20px;
    font-size:20px;
    z-index:50;
}

/* floating hearts */
.heart{
    position:absolute;
    animation:float 6s linear infinite;
}

@keyframes float{
    from{transform:translateY(100vh);opacity:1}
    to{transform:translateY(-10vh);opacity:0}
}

/* gallery */
.gallery img{
    width:70px;
    height:70px;
    border-radius:10px;
    margin:4px;
}

/* fireworks */
.fire{
    position:absolute;
    font-size:20px;
    animation:boom 1s linear forwards;
}

@keyframes boom{
    to{transform:scale(3);opacity:0}
}

#timer{
    font-size:13px;
    color:#666;
    margin:8px;
}
</style>
</head>

<body>

<!-- PASSWORD SCREEN -->
<div id="lockScreen">
    <h2>Enter our special name ❤️</h2>
    <input type="password" id="pass" placeholder="Hint: Her name"/>
    <br><br>
    <button onclick="unlock()">Open 💕</button>
</div>

<!-- BACKGROUND MUSIC -->
<audio autoplay loop>
    <source src="https://cdn.pixabay.com/audio/2022/03/15/audio_115b9b7f59.mp3" type="audio/mp3">
</audio>

<!-- MAIN CONTENT -->
<div class="container" id="main">

    <img src="https://i.imgur.com/9Yj7G5Q.png">

    <h2 id="typing"></h2>

    <div id="timer"></div>

    <button id="yes">Yes 💍</button>
    <button id="no">No 😜</button>

    <!-- Gallery -->
    <div class="gallery">
        <img src="https://i.imgur.com/9Yj7G5Q.png">
        <img src="https://i.imgur.com/9Yj7G5Q.png">
        <img src="https://i.imgur.com/9Yj7G5Q.png">
    </div>

    <br>
    <button onclick="showLetter()">Open Love Letter 💌</button>
</div>

<!-- POPUP -->
<div class="popup" id="popup">
    Thank you Anu 🥰❤️  
    I am forever thankful for you 💖  
    Let's build our happy family together 🎈🎆
</div>

<script>

/* PASSWORD LOCK */
function unlock(){
    if(document.getElementById("pass").value.toLowerCase()=="anu"){
        lockScreen.style.display="none";
        main.style.display="block";
    }
}

/* TYPING ANIMATION */
const text="Anu, will you be my wife for our future life to lead a happy family? ❤️";
let i=0;
setInterval(()=>{
    if(i<text.length){
        document.getElementById("typing").innerHTML+=text[i];
        i++;
    }
},50);

/* NO BUTTON RUN AWAY */
const noBtn=document.getElementById("no");
noBtn.addEventListener("mouseover",()=>{
    noBtn.style.left=Math.random()*300+"px";
    noBtn.style.top=Math.random()*300+"px";
});

/* YES CLICK */
document.getElementById("yes").onclick=()=>{
    popup.style.display="block";
    balloons();
    fireworks();
}

/* HEARTS */
setInterval(()=>{
    const h=document.createElement("div");
    h.innerHTML="❤️";
    h.className="heart";
    h.style.left=Math.random()*100+"vw";
    document.body.appendChild(h);
    setTimeout(()=>h.remove(),6000);
},300);

/* BALLOONS */
function balloons(){
    for(let i=0;i<25;i++){
        const b=document.createElement("div");
        b.innerHTML="🎈";
        b.className="heart";
        document.body.appendChild(b);
    }
}

/* FIREWORKS */
function fireworks(){
    for(let i=0;i<40;i++){
        const f=document.createElement("div");
        f.innerHTML="✨";
        f.className="fire";
        f.style.left=Math.random()*100+"vw";
        f.style.top=Math.random()*100+"vh";
        document.body.appendChild(f);
    }
}

/* LOVE LETTER */
function showLetter(){
    alert("My dearest Anu ❤️\n\nFrom the day you came into my life, everything became beautiful. I promise to love, protect, and care for you forever. Will you be mine for life? 💕");
}

/* COUNTDOWN */
const meet=new Date("2026-03-01").getTime();
setInterval(()=>{
    const now=new Date().getTime();
    const d=meet-now;
    if(d>0){
        document.getElementById("timer").innerHTML="Countdown: "+Math.floor(d/(1000*60*60*24))+" days left 💞";
    }
},1000);

</script>

</body>
</html>